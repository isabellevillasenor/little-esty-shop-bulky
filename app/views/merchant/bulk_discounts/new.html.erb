<%= render partial: 'navbar' %>

<%= render partial: 'form', locals: { method: :post, url: merchant_bulk_discounts_path(@merchant), class: 'merchant_discount_form', local: true, model: @bulk_discount } %>